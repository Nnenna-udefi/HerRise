import { ShellLayout } from "@/components/shared/organism/sharedLayout";
import { createMetaData } from "@/utils/helpers";

export const metadata = createMetaData({
  title: "About Us | HerRise",
  description:
    "Unlock your potential with personalized coaching and therapy services.",
});

export default ShellLayout;
